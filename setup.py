from setuptools import setup


setup(
    name='example',
    version='0.1.0',
    description='Moving items around a grid',
    long_description=open('README.rst').read(),
    license='BSD',
    install_requires=[],
    py_modules=['example'],
    include_package_data=True,
    zip_safe=False,
    classifiers=[
    ]
)
